import { CpuppercasePipe } from './cpuppercase.pipe';

describe('CpuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new CpuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
