import { CplowercasePipe } from './cplowercase.pipe';

describe('CplowercasePipe', () => {
  it('create an instance', () => {
    const pipe = new CplowercasePipe();
    expect(pipe).toBeTruthy();
  });
});
